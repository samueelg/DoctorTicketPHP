services:

  nginx:
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf

  app:
    volumes:
      - ./:/var/www

  reverb:
    volumes:
      - ./:/var/www